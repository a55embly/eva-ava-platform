# Docker images

The API and worker use separate images built from the same source tree.

Add a Hermes image only after selecting and pinning an upstream version, verifying its license, and documenting its configuration and upgrade path. Do not use an unpinned remote install script in a production image.
